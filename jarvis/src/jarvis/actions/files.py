"""File executor (bootstrap skeleton).

Design: create_doc new-only (never overwrite/edit/delete), open_file_dir via
file manager (RF-9). Real implementation lands in PR4 (executors).
"""

from __future__ import annotations


def create_doc() -> None:
    """Bootstrap stub — real implementation lands in PR4 (executors)."""
    raise NotImplementedError("jarvis.actions.files.create_doc: implemented in PR4 (executors)")
