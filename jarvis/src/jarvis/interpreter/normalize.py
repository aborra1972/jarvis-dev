"""Rioplatense normalization (bootstrap skeleton).

Design: lowercase, strip accents/punctuation, collapse whitespace, strip
leading wake-word token(s). Real implementation lands in PR2 (interpreter).
"""

from __future__ import annotations


def normalize() -> None:
    """Bootstrap stub — real implementation lands in PR2 (interpreter)."""
    raise NotImplementedError("jarvis.interpreter.normalize.normalize: implemented in PR2 (interpreter)")
