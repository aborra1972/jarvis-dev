"""Intent JSON schema + validation (bootstrap skeleton).

Design: the 15-intent allowlist schema with entities (repo/app/query/text/url/
engine) and confidence, validated per executor. Real implementation lands in
PR2 (interpreter).
"""

from __future__ import annotations


def validate() -> None:
    """Bootstrap stub — real implementation lands in PR2 (interpreter)."""
    raise NotImplementedError("jarvis.interpreter.schema.validate: implemented in PR2 (interpreter)")
