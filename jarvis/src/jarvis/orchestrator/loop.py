"""Main loop wiring (bootstrap skeleton).

Design: capture → STT → interpreter → executor → TTS, ack >3s, offline degrade.
Real implementation lands in PR6 (E2E + polish).
"""

from __future__ import annotations


def run() -> None:
    """Bootstrap stub — real implementation lands in PR6 (E2E + polish)."""
    raise NotImplementedError("jarvis.orchestrator.loop.run: implemented in PR6 (E2E + polish)")
