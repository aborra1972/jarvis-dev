# Tasks: Jarvis Voice Turn-Taking Improvement

Implementation authorization: Slices 1, 2, and 3 explicitly granted by the user for stacked-to-main apply. Apply must pause before edits outside the authorized slice surfaces.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 850-1,150 total across 3 stacked slices; target Slice 1 <250, Slice 2 300-400, Slice 3 300-400 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 capture boundaries + speaker queue cancellation → PR 2 unified readiness barrier + confirmation/reask + follow-up anchoring → PR 3 active lifecycle + exact goodbye + off/fresh wake/stale work + barge-in integration |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

## Stacked-to-main dependency map

```text
main
└─ Slice 1: capture boundaries + speaker queue cancellation
   └─ Slice 2: unified readiness barrier + confirmation/reask + follow-up anchoring
      └─ Slice 3: active lifecycle + exact goodbye + off/fresh wake/stale work + barge-in integration
```

Global verification command for every slice: `jarvis/.venv/bin/pytest`.
Strict TDD is required by `openspec/config.yaml`: each implementation session records RED, GREEN, TRIANGULATE, and REFACTOR evidence before moving to the next task.

## Slice 1 — Capture boundaries + speaker queue cancellation

Start boundary: branch from `main`; only low-level audio capture and speaker interruption seams.
Finish boundary: capture onset/endpoint behavior and TTS queue-drain cancellation are proven without orchestrator rewiring.
Rollback boundary: revert Slice 1 branch/PR to restore previous `jarvis/src/jarvis/audio/*` behavior.
Dependencies: none.
Non-goals: no readiness-barrier extraction, no confirmation deadline routing, no lifecycle goodbye/off changes, no provider/AEC/streaming redesign.

- [x] RED: add failing capture tests in `jarvis/tests/unit/test_audio_capture.py` for initial silence followed by speech, trailing silence only after onset, bounded pre-roll during indefinite ordinary onset wait, confirmation finite deadline cancellation, and transient `NO_FRAME` not completing an utterance; evidence is failing pytest output for those tests only; acceptance is failures match `voice-pipeline` onset/cancellation scenarios; rollback is deleting the new tests from this slice; depends on current `jarvis/src/jarvis/audio/capture.py`; non-goals are orchestrator and confirmation changes. <!-- sdd-owner: implementation -->
- [x] GREEN: minimally adjust or preserve `jarvis/src/jarvis/audio/capture.py` (`gather_utterance_result`, `CaptureMode`, `CaptureResult`, `SoundDeviceCapturer.flush`, VAD seams) so the RED tests pass while keeping finite post-onset caps and bounded idle retention; evidence is targeted pytest pass for `jarvis/tests/unit/test_audio_capture.py`; acceptance is no fabricated transcript on cancellation/failure and no unlimited recording; rollback is reverting the capture.py hunk; depends on Slice 1 RED tests; non-goals are new config knobs unless required by tests. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add adjacent regression tests in `jarvis/tests/unit/test_audio_capture.py` for audio-device failure vs transient no-frame and post-wake finite initial-silence policy independent from trailing-silence cutoff; evidence is fail-then-pass pytest output; acceptance is device failures report failure and no command-dispatch-shaped result is produced; rollback is removing the added boundary tests and associated minimal production hunk; depends on Slice 1 GREEN; non-goals are manual audio instrumentation. <!-- sdd-owner: implementation -->
- [x] RED: add failing speaker cancellation tests in `jarvis/tests/unit/test_audio_pipeline.py` proving `PiperSpeaker.interrupt()` stops active playback and drains queued pending speech, and stale queued speech cannot resume after barge-in-style interruption; evidence is failing targeted pytest output; acceptance is active and queued TTS are both covered; rollback is deleting the new tests; depends on current `jarvis/src/jarvis/audio/pipeline.py`; non-goals are orchestrator barge-in routing. <!-- sdd-owner: implementation -->
- [x] GREEN: minimally adjust or preserve `jarvis/src/jarvis/audio/pipeline.py` (`PiperSpeaker.interrupt`, `is_playing`, queue handling, fakes as needed) so interruption stops playback and drains pending queue entries; evidence is targeted pytest pass for `jarvis/tests/unit/test_audio_pipeline.py`; acceptance is interrupt is idempotent and does not require playback completion; rollback is reverting the pipeline.py hunk; depends on speaker RED tests; non-goals are epoch-token producer cancellation beyond existing queue drain. <!-- sdd-owner: implementation -->
- [x] REFACTOR: clean duplicated capture/speaker test fakes only inside `jarvis/tests/unit/test_audio_capture.py` and `jarvis/tests/unit/test_audio_pipeline.py`, then run `jarvis/.venv/bin/pytest jarvis/tests/unit/test_audio_capture.py jarvis/tests/unit/test_audio_pipeline.py`; evidence is before/after diff plus full targeted pass; acceptance is Slice 1 stays under the 400-line budget target where possible; rollback is reverting refactor-only hunks; depends on all Slice 1 GREEN/TRIANGULATE tasks; non-goals are behavior changes. <!-- sdd-owner: implementation -->

## Slice 2 — Unified readiness barrier + confirmation/reask + follow-up anchoring

Start boundary: branch from merged Slice 1.
Finish boundary: every ordinary, follow-up, reask, and confirmation capture enters through one readiness seam with prompt completion, cooldown, flushes, mic readiness, and cancellation fail-closed behavior.
Rollback boundary: revert Slice 2 branch/PR to restore prior orchestrator sequencing while keeping Slice 1.
Dependencies: Slice 1 merged.
Non-goals: no exact-goodbye lifecycle expansion, no GUI-off cancellation rewrite except fail-closed barrier checks, no destructive safety weakening.

- [x] RED: add failing barrier-order tests in `jarvis/tests/unit/test_loop.py` around `jarvis/src/jarvis/orchestrator/loop.py` proving ordinary and follow-up capture wait for `speaker.is_playing() == False`, post-playback cooldown, wake flush, capture flush, and mic start before capture; evidence is failing pytest output with fake speaker/wake/capturer call ordering; acceptance is fixed sleeps alone do not satisfy readiness; rollback is deleting the new tests; depends on Slice 1; non-goals are confirmation deadline changes. <!-- sdd-owner: implementation -->
- [x] GREEN: refactor `jarvis/src/jarvis/orchestrator/loop.py` by extracting `_prepare_ordinary_capture()` into a unified readiness helper/seam used by ordinary and follow-up capture, preserving `_Context`, epoch, and existing cooldown behavior; evidence is targeted pass for new `test_loop.py` readiness tests; acceptance is capture never starts while TTS is queued/active and cancellation/off fails closed; rollback is reverting the loop.py helper hunk; depends on barrier RED tests; non-goals are public API churn outside orchestrator unless tests require protocol typing. <!-- sdd-owner: implementation -->
- [x] RED: add failing confirmation readiness tests in `jarvis/tests/unit/test_confirm.py` and/or `jarvis/tests/unit/test_loop.py` proving confirmation prompt completion plus the same readiness barrier starts the 15s window, exactly `T+15s` expires, stale token/epoch and GUI off fail closed, and retry does not renew authorization; evidence is failing targeted pytest output; acceptance maps to `system-control` prompt-completion and cancellation scenarios; rollback is deleting the tests; depends on unified barrier seam; non-goals are changing golden-gate policy. <!-- sdd-owner: implementation -->
- [x] GREEN: update `jarvis/src/jarvis/orchestrator/confirm.py`, `jarvis/src/jarvis/orchestrator/contracts.py` only if needed, and `jarvis/src/jarvis/orchestrator/loop.py` call sites so confirmation capture uses prompt completion plus barrier readiness and deadline anchoring; evidence is targeted pass for `jarvis/tests/unit/test_confirm.py` and relevant `test_loop.py` tests; acceptance is late affirmative and stale authorization cannot execute; rollback is reverting confirmation/contract/loop hunks; depends on confirmation RED tests; non-goals are destructive executor changes beyond validity checks at existing seams. <!-- sdd-owner: implementation -->
- [x] RED: add failing reask and follow-up tests in `jarvis/tests/unit/test_loop.py` proving reask prompts use the same readiness barrier, long TTS does not consume follow-up duration, and no capture starts during active or queued prompt/output; evidence is failing targeted pytest output; acceptance maps to `voice-pipeline` reask and follow-up scenarios; rollback is deleting the tests; depends on ordinary/follow-up barrier GREEN; non-goals are active lifecycle arbitrary-pause hardening reserved for Slice 3. <!-- sdd-owner: implementation -->
- [x] GREEN: update `jarvis/src/jarvis/orchestrator/loop.py` reask and follow-up paths so the follow-up window is anchored to actual playback completion and reask response capture uses the unified barrier; evidence is targeted pass for `jarvis/tests/unit/test_loop.py`; acceptance is configured follow-up duration begins after audible playback completion; rollback is reverting loop.py follow-up/reask hunks; depends on reask/follow-up RED tests; non-goals are goodbye/off/fresh-wake policy changes. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add boundary tests in `jarvis/tests/unit/test_confirm.py` and `jarvis/tests/unit/test_loop.py` for prompt failure/unavailable completion evidence, barrier timeout/loss of readiness, and cancellation between barrier success and capture start; evidence is fail-then-pass targeted pytest output; acceptance is all ambiguous readiness failures fail closed and preserve ordinary conversation where required; rollback is reverting boundary tests and smallest behavior hunk; depends on Slice 2 GREEN tasks; non-goals are manual latency tuning. <!-- sdd-owner: implementation -->
- [x] REFACTOR: consolidate fake clocks, fake speakers, wake/capturer order assertions, and readiness helper naming in `jarvis/tests/unit/test_loop.py`, `jarvis/tests/unit/test_confirm.py`, and `jarvis/src/jarvis/orchestrator/loop.py`; run `jarvis/.venv/bin/pytest jarvis/tests/unit/test_loop.py jarvis/tests/unit/test_confirm.py`; evidence is targeted pass and Slice 2 diff review under 400 lines where possible; acceptance is one readiness seam remains discoverable and call sites are dependency ordered; rollback is reverting refactor-only hunks; depends on all Slice 2 tests passing; non-goals are lifecycle behavior expansion. <!-- sdd-owner: implementation -->

## Slice 3 — Active lifecycle + exact goodbye + off/fresh wake/stale work + barge-in integration

Start boundary: branch from merged Slice 2.
Finish boundary: approved active conversation lifecycle, exact standalone goodbye, GUI-off precedence, fresh-wake boundaries, stale work invalidation, and barge-in integration are test-proven.
Rollback boundary: revert Slice 3 branch/PR to keep lower-level capture/readiness improvements from Slices 1-2.
Dependencies: Slices 1 and 2 merged.
Non-goals: no persistence schema redesign, no history-shape changes except preventing stale successful entries, no broad dictation rewrite, no hardware/AEC/streaming provider work.

- [x] RED: add failing exact-goodbye interpreter tests in `jarvis/tests/unit/test_interpreter.py` and dictation tests in the concrete existing dictation test file under `jarvis/tests/unit/` proving only exact standalone `terminamos` and `hasta luego` emit lifecycle goodbye, while quoted/embedded/negated/reported occurrences remain ordinary or dictated content; evidence is failing targeted pytest output; acceptance maps to `command-interpreter` goodbye and dictation scenarios; rollback is deleting tests; depends on Slice 2; non-goals are LLM routing changes. <!-- sdd-owner: implementation -->
- [x] GREEN: minimally adjust or preserve `jarvis/src/jarvis/interpreter/interpreter.py` (`GOODBYE_PHRASES`, `is_goodbye`, `resolve_intent`) and `jarvis/src/jarvis/interpreter/dictation.py` so exact standalone goodbye has deterministic lifecycle precedence without changing existing send/clear/exit dictation controls; evidence is targeted pass for interpreter/dictation tests; acceptance is goodbye is not destructive authorization and ambiguous content fails closed; rollback is reverting interpreter/dictation hunks; depends on exact-goodbye RED tests; non-goals are orchestrator close behavior. <!-- sdd-owner: implementation -->
- [x] RED: add failing lifecycle close tests in `jarvis/tests/unit/test_loop.py` proving exact goodbye in an active conversation sends text and voice acknowledgement, returns to wake-name standby, keeps process powered on, invalidates pending confirmation/capture/execution/follow-up/queued response work, and requires fresh wake afterward; evidence is failing targeted pytest output; acceptance maps to `assistant-lifecycle` active conversation scenarios; rollback is deleting tests; depends on exact-goodbye GREEN; non-goals are GUI off and restart handling. <!-- sdd-owner: implementation -->
- [x] GREEN: update `jarvis/src/jarvis/orchestrator/loop.py` (`_Context`, `_activate_epoch`, `_operation_is_current`, `_close_goodbye`, `State.IDLE`, `State.LISTENING`, `State.CONFIRMING`) so goodbye cancels current operation/authorization, clears follow-up state, acknowledges, and returns to standby requiring fresh wake; evidence is targeted pass for lifecycle close tests; acceptance is late results cannot execute, authorize, reopen follow-up, or speak; rollback is reverting loop.py lifecycle hunks; depends on lifecycle close RED tests; non-goals are off precedence changes. <!-- sdd-owner: implementation -->
- [x] RED: add failing GUI-off/fresh-wake precedence tests in `jarvis/tests/unit/test_loop.py` proving GUI off wins over goodbye, dictation, confirmation, output, and pending work; off cancels capture/output without goodbye acknowledgement; restart/off/conversation close require fresh wake; `power_off_self` still uses destructive confirmation; evidence is failing targeted pytest output; acceptance maps to `assistant-lifecycle` and `system-control` scenarios; rollback is deleting tests; depends on lifecycle GREEN; non-goals are changing `power_off_self` executor semantics. <!-- sdd-owner: implementation -->
- [x] GREEN: update `jarvis/src/jarvis/orchestrator/loop.py` (`_apply_switch`, off/restart transitions, operation invalidation) and speaker interactions so GUI off uses interrupt/cancel semantics rather than flushing stale output, invalidates authorization/work, suppresses goodbye ack, and preserves `power_off_self`; evidence is targeted pass for GUI-off tests plus `jarvis/tests/unit/test_confirm.py`; acceptance is off leaves assistant off and ordinary speech is ignored until fresh wake; rollback is reverting off-transition hunks; depends on GUI-off RED tests; non-goals are GUI UI cleanup. <!-- sdd-owner: implementation -->
- [x] RED: add failing stale-epoch/history tests in `jarvis/tests/unit/test_loop.py` and the concrete existing history/log test file under `jarvis/tests/unit/` proving late capture, STT, interpretation, confirmation, execution, and TTS results from inactive epochs do not dispatch commands, enqueue speech, update follow-up, or record successful command-history entries; evidence is failing targeted pytest output; acceptance maps to `voice-pipeline` active epoch and `assistant-lifecycle` deletable logs scenarios; rollback is deleting tests; depends on lifecycle/off GREEN; non-goals are history schema or redaction redesign. <!-- sdd-owner: implementation -->
- [x] GREEN: update `jarvis/src/jarvis/orchestrator/loop.py`, `jarvis/src/jarvis/orchestrator/contracts.py` only if needed, and existing history call sites only if required so stale epoch/token checks gate all late result decision points and prevent successful history writes; evidence is targeted pass for stale-epoch/history tests; acceptance is existing history schema and redaction behavior are unchanged; rollback is reverting stale-token/history hunks; depends on stale-epoch RED tests; non-goals are persistence redesign. <!-- sdd-owner: implementation -->
- [x] RED: add failing barge-in integration tests in `jarvis/tests/unit/test_loop.py` and `jarvis/tests/unit/test_audio_pipeline.py` proving when `BARGE_IN_ENABLED` is true a valid wake during speech stops active playback, drains queued TTS for the interrupted epoch, flushes buffers, and only enters listening after the readiness barrier; evidence is failing targeted pytest output; acceptance maps to `voice-pipeline` barge-in scenario; rollback is deleting tests; depends on Slice 1 speaker cancellation and Slice 2 barrier; non-goals are enabling barge-in by default or adding AEC. <!-- sdd-owner: implementation -->
- [x] GREEN: update `jarvis/src/jarvis/orchestrator/loop.py` barge-in path and `jarvis/src/jarvis/config.py` only if tests prove configuration seams are missing, preserving `BARGE_IN_ENABLED = False` by default and routing enabled barge-in through interrupt plus readiness barrier; evidence is targeted pass for barge-in tests; acceptance is stale queued speech cannot resume and disabled barge-in keeps mic closed during playback; rollback is reverting barge-in/config hunks; depends on barge-in RED tests; non-goals are provider or wake-model redesign. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add boundary regression tests across `jarvis/tests/unit/test_loop.py`, `jarvis/tests/unit/test_confirm.py`, and `jarvis/tests/unit/test_interpreter.py` for goodbye during confirmation, off during goodbye acknowledgement, embedded goodbye during dictation, fresh wake replacing an active epoch, exact `T+15s` confirmation expiry after lifecycle churn, and `power_off_self` distinction; evidence is fail-then-pass targeted pytest output; acceptance is all precedence rules fail closed without safety regression; rollback is reverting boundary tests and smallest behavior hunk; depends on all Slice 3 GREEN tasks; non-goals are broad conversation memory redesign. <!-- sdd-owner: implementation -->
- [x] REFACTOR: consolidate epoch/token helper naming, lifecycle fakes, and repeated stale-result assertions in `jarvis/src/jarvis/orchestrator/loop.py`, `jarvis/src/jarvis/orchestrator/contracts.py` if touched, and related unit tests; run `jarvis/.venv/bin/pytest`; evidence is full-suite pass and Slice 3 diff review near or under 400 lines where possible; acceptance is no unrelated formatting or UI cleanup enters the slice; rollback is reverting refactor-only hunks; depends on all Slice 3 tests passing; non-goals are changing accepted behavior. <!-- sdd-owner: implementation -->

## Final stacked verification before apply completion

- [x] Verify the whole chain from the Slice 3 tip with `jarvis/.venv/bin/pytest`, inspect per-slice diffs for the 400-line budget, confirm no source/test files outside the planned paths changed, and record RED/GREEN/TRIANGULATE/REFACTOR evidence for each slice before requesting review; acceptance is three reviewable stacked-to-main PR candidates and no implementation authorization implied by this task artifact; rollback is dropping the unmerged stack branches in reverse order; depends on all slice tasks; non-goals are opening PRs, merging, or applying source changes without explicit authorization. <!-- sdd-owner: implementation -->
