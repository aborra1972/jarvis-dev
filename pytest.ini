[pytest]
testpaths = jarvis/tests
# E2E smoke tests require the real spike binaries and are opt-in.
addopts = -m "not e2e"
markers =
    e2e: end-to-end smoke against real spike binaries (run with pytest -m e2e)
